# Git shortcuts
alias gs="git status --porcelain"
alias gl="git pull"
alias gc="git commit"
alias glg="git log"
alias gr='git rebase'

# Docker aliases
alias dc=docker compose

# For MacOS only
# alias kape="caffeinate -d"
